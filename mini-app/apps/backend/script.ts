import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const rows = await prisma.$queryRawUnsafe('SELECT table_name, column_name, data_type FROM information_schema.columns WHERE table_name IN (''users'', ''tournament_participants'', ''balances'') AND column_name IN (''id'', ''user_id'');'); console.log(rows); } main().finally(() => prisma.$disconnect());
