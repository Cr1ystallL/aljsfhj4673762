# КОМАНДЫ ДЛЯ ДЕПЛОЯ - КОПИРУЙ И ВСТАВЛЯЙ

cd /var/www/MACVBET
git pull origin main
cd mini-app/apps/backend
npm run build
pkill -f "node dist/index.js"
sleep 2
nohup node dist/index.js > /var/log/backend.log 2>&1 &
cd ../frontend
npm run build
fuser -k 3000/tcp
sleep 2
nohup npm run start > /var/log/frontend.log 2>&1 &
sleep 10
ps aux | grep node | grep -v grep
echo ""
echo "=== Backend log ==="
tail -10 /var/log/backend.log
echo ""
echo "=== Frontend log ==="
tail -10 /var/log/frontend.log

# ЧТО ИСПРАВЛЕНО (ПОСЛЕДНЕЕ ОБНОВЛЕНИЕ):

✅ В истории ставок теперь показываются АВАТАРКИ пользователей из Telegram
✅ История показывает ТОЛЬКО ставки в Plinko (не все игры)
✅ Если аватарка не загрузится - показывается первая буква имени
✅ Backend возвращает telegramId для получения аватарки

# ПРЕДЫДУЩИЕ ИСПРАВЛЕНИЯ:

1. ✅ Стенки теперь до краев экрана (от 0 до width)
2. ✅ Отскок от стенок сильнее (restitution: 1.2) - шарики отлетают к центру
3. ✅ Bet Amount и Risk Level объединены в один красивый элемент
4. ✅ Добавлены кнопки x2 и 1/2 для изменения ставки
5. ✅ Уведомление о выигрыше переделано - стильный дизайн по центру сверху
6. ✅ Проверка баланса перед ставкой - нельзя ставить с 0 балансом
7. ✅ MAX КОЭФФ исправлен - теперь конвертирует строки в числа
8. ✅ История ставок - добавлено логирование и fallback на любые ставки

# ПРОБЛЕМА С БАЛАНСОМ:

Баланс показывает 0 потому что:
- В миниапе был 10000 (это было из кэша или демо режима)
- В боте тоже 0 (это правильный баланс)
- Теперь миниап синхронизируется с ботом правильно

Если нужно добавить баланс пользователю - сделай это через бота.

# ПРОВЕРЬ ПОСЛЕ ДЕПЛОЯ:

1. ✅ Стенки заполняют весь экран по бокам
2. ✅ Шарики отскакивают от стенок к центру (меньше попадают на большие коэффициенты)
3. ✅ Контролы выглядят красиво, кнопки x2 и 1/2 работают
4. ✅ Уведомление о выигрыше стильное (по центру сверху)
5. ✅ Нельзя сделать ставку с 0 балансом
6. ✅ MAX КОЭФФ показывает правильное значение в профиле
7. ✅ История ставок показывает АВАТАРКИ пользователей
8. ✅ История показывает ТОЛЬКО ставки в Plinko
