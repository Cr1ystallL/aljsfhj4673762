# КОМАНДЫ ДЛЯ ДЕПЛОЯ - КОПИРУЙ И ВСТАВЛЯЙ

cd /var/www/MACVBET
git pull origin main
cd mini-app/apps/backend
npm run build
pkill -f "node dist/index.js"
sleep 2
nohup node dist/index.js > /var/log/backend.log 2>&1 &
cd ../frontend
npm run build
fuser -k 3000/tcp
sleep 2
nohup npm run start > /var/log/frontend.log 2>&1 &
sleep 10
ps aux | grep node | grep -v grep
echo ""
echo "=== Backend log ==="
tail -10 /var/log/backend.log
echo ""
echo "=== Frontend log ==="
tail -10 /var/log/frontend.log

# ЧТО ИСПРАВЛЕНО (ПОСЛЕДНЕЕ БОЛЬШОЕ ОБНОВЛЕНИЕ):

## ✅ Стенки Plinko
- Стенки теперь ДАЛЬШЕ от пирамиды (wallOffset = GAP * 3)
- Диагональные, следуют форме пирамиды
- Цвета из Monopo Saigon (зеленый и оранжевый градиент)

## ✅ Уведомление о выигрыше
- УБРАНО полностью (как просили)
- Только подсветка bucket при попадании шарика

## ✅ История ставок игрока
- Добавлена секция "Последние ставки" под кнопкой Drop Ball
- Показывает последние 10 ставок ТЕКУЩЕГО ИГРОКА
- Формат: Ставка | Множитель | Выигрыш
- Backend endpoint: /api/games/plinko/my-history

## ✅ Live Bets
- Перенесена НИЖЕ истории игрока
- Показывает случайные ставки от ВСЕХ игроков
- С аватарками из Telegram

## ✅ Цветовая схема Monopo Saigon
- Применены цвета из референса
- Стенки: rgba(160, 224, 171) и rgba(255, 172, 46)
- Border radius: 10px для карточек, 75px для кнопок
- Midnight Canvas (#000000) фон
- Frost White (#ffffff) текст

## ⚠️ MAX КОЭФФ - ЕЩЕ НЕ ИСПРАВЛЕН
- Проблема: показывает 4.0 вместо 110
- Нужно проверить почему metadata.multiplier не сохраняется правильно
- Или проверить почему не читается из базы

# ПРЕДЫДУЩИЕ ИСПРАВЛЕНИЯ:

1. ✅ Bet Amount и Risk Level объединены в один элемент
2. ✅ Добавлены кнопки x2 и 1/2 для изменения ставки
3. ✅ Проверка баланса перед ставкой
4. ✅ Аватарки пользователей в истории
5. ✅ История показывает ТОЛЬКО ставки в Plinko

# ПРОВЕРЬ ПОСЛЕ ДЕПЛОЯ:

1. ✅ Стенки дальше от пирамиды
2. ✅ Уведомление о выигрыше НЕ показывается
3. ✅ Под кнопкой Drop Ball - история последних 10 ставок игрока
4. ✅ Ниже - Live Bets от всех игроков
5. ⚠️ MAX КОЭФФ в профиле (нужно проверить почему 4.0 вместо 110)
6. ✅ Цвета Monopo Saigon применены
